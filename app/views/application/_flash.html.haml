- flash.each do |key, msg|
  .alert.alert-dismissible.fade.in{ class: "alert-#{key}" }
    %button.close{ 'aria-label' => 'Close', 'data-dismiss' => 'alert',
    :type => 'button' }
      %span{ 'aria-hidden' => 'true' } ×
    = msg
- flash.clear
